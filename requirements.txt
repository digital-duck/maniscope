# Core dependencies for Maniscope engine
numpy>=1.21.0
networkx>=2.6.0
scikit-learn>=1.0.0
sentence-transformers>=2.2.0

# PyTorch with CUDA 12.1 support
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Optional: Advanced optimizations (v2, v2o)
scipy>=1.7.0
joblib>=1.0.0

# Optional: GPU acceleration (v2, v2o)
# faiss-gpu>=1.7.0  # Uncomment for GPU support
# faiss-cpu>=1.7.0  # Uncomment for CPU-only FAISS

# Optional: Flash Attention for performance
# flash-attn>=2.0.0  # Install with: pip install flash-attn --no-build-isolation

# Streamlit evaluation app dependencies
streamlit>=1.25.0
pandas>=1.3.0
plotly>=5.0.0
xlsxwriter>=3.0.0
streamlit-echarts>=0.4.0  # Required for Grid Search 3D visualizations

# Optional: Additional rerankers for comparison
FlagEmbedding>=1.0.0
openai>=1.0.0  # For LLM reranker via API
einops>=0.6.0  # Required for Jina Reranker v2
hnswlib>=0.7.0  # Required for HNSW reranker baseline

# Optional: Dataset preparation
mteb>=1.0.0
beir>=1.0.0
datasets>=2.0.0

# PDF processing for custom datasets
docling>=1.0.0
